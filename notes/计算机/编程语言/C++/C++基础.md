# 常用技能
## 如何声明一个新数组

### 🧩 一、静态数组（编译时长度固定）
✅ 语法
```cpp
类型 数组名[元素个数];
```
✅ 示例
```cpp
int arr[5];                      // 声明一个包含 5 个 int 的数组（未初始化）
int nums[5] = {1, 2, 3, 4, 5};   // 声明并初始化
int zeros[5] = {0};              // 全部初始化为 0
```
📌 特点
- 数组长度 **必须是常量表达式**
    
- 大小在 **编译时确定**，不可动态改变
    
- 存储在 **栈内存** 中
### 💡 二、动态数组（运行时长度可变）
✅ 示例
```cpp
int n;
cin >> n;
int* arr = new int[n];  // 在堆上动态分配数组

for (int i = 0; i < n; i++) {
    arr[i] = i + 1;
}

delete[] arr;  // ✅ 使用完必须释放内存
```
📌 特点
- 长度可在 **运行时决定**
    
- 存储在 **堆内存**
    
- 必须使用 `delete[]` 释放，否则会 **内存泄漏**
    

### 🧰 三、使用 `std::vector`（推荐现代做法）
✅ 示例
```cpp
#include <vector>
using namespace std;

int main() {
    int n;
    cin >> n;

    vector<int> arr(n);            // 长度为 n，默认值为 0
    vector<int> arr2 = {1, 2, 3};  // 初始化

    arr[0] = 42;
    arr.push_back(99);             // 动态扩容
}
```
📌 特点
- 自动管理内存，无需 `delete`
    
- 支持动态扩容
    
- 可直接使用 STL 算法
    
- ✅ **现代 C++ 推荐使用**

### 🧠 四、使用 `std::array`（C++11 起）
✅ 示例
```cpp
#include <array>
using namespace std;

array<int, 5> arr = {1, 2, 3, 4, 5};
```
📌 特点
- 长度固定（编译时确定）
    
- 存储在栈上
    
- 支持 `arr.size()`、`arr.begin()` 等 STL 接口
    

### 🧮 五、二维数组声明方式
✅ 静态二维数组
```cpp
int matrix[3][4];                        // 3行4列
int grid[2][3] = {{1, 2, 3}, {4, 5, 6}};
```
✅ 动态二维数组（使用指针 + new）
```cpp
int rows = 3, cols = 4;
int** matrix = new int*[rows];
for (int i = 0; i < rows; i++) {
    matrix[i] = new int[cols];
}

// 使用
matrix[1][2] = 10;

// 释放
for (int i = 0; i < rows; i++)
    delete[] matrix[i];
delete[] matrix;
```
### ✅ 最推荐：`vector<vector<int>>`

语法：`vector<vector<int>> arr(rows, vector<int>(cols));`

```cpp
vector<vector<int>> matrix(3, vector<int>(4, 0));
matrix[1][2] = 10;
```

### 📊 总结对比表

|类型|长度是否可变|是否需手动释放|推荐度|特点|
|---|---|---|---|---|
|`int arr[5];`|❌ 否|❌ 否|⭐|栈上分配，速度快但不可变|
|`int* arr = new int[n];`|✅ 是|✅ 是|⚠️|手动管理内存，易错|
|`std::vector<int>`|✅ 是|❌ 否|🌟🌟🌟🌟🌟|最常用、最安全|
|`std::array<int, N>`|❌ 否|❌ 否|🌟🌟🌟|固定长度但功能强|
|`int** arr = new int*[n];`|✅ 是（二维）|✅ 是|⚠️|代码繁琐，易内存泄漏|
|`vector<vector<int>>`|✅ 是|❌ 否|🌟🌟🌟🌟🌟|二维数组最推荐写法|
